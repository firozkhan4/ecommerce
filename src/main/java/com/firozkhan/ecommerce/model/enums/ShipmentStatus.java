package com.firozkhan.ecommerce.model.enums;

public enum ShipmentStatus {
    PENDING,
    SHIPPED,
    IN_TRANSIT,
    DELIVERED,
    RETURNED,
    CANCELLED,
    OUT_FOR_DELIVERY,
    DELAYED,
    FAILED_ATTEMPT,
    UNKNOWN,
    FAILED
}
