package com.firozkhan.ecommerce.modules.shipment.domain.enums;

public enum ShipmentStatus {
    PENDING,
    SHIPPED,
    IN_TRANSIT,
    DELIVERED,
    RETURNED,
    CANCELLED,
    OUT_FOR_DELIVERY,
    DELAYED,
    FAILED_ATTEMPT,
    UNKNOWN,
    FAILED
}
